# 复制本文件为 .env.oss，然后填写自己的 OSS 信息。
# .env.oss 里不要提交 AccessKey，也不要发给开发。

# 必填：Bucket 名称，例如 lianxu-laowu-prototype
OSS_BUCKET=""

# 可选：如果想部署到 Bucket 下的子目录，例如 prototype/v1，就填写。
# 如果文件直接放 Bucket 根目录，保持为空。
OSS_PREFIX=""

# 可选：Endpoint，例如 https://oss-cn-hangzhou.aliyuncs.com
# 如果你已经通过 ossutil config 配置过默认 endpoint，可以留空。
OSS_ENDPOINT=""

# 可选：最终给开发访问的域名，用于脚本输出提示。
# 例如 https://lianxu-laowu-prototype.oss-cn-hangzhou.aliyuncs.com
PUBLIC_URL=""
